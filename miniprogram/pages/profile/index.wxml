<!-- profile/index.wxml -->
<view class="container">
  <!-- 用户信息 -->
  <view class="user-header">
    <block wx:if="{{userInfo}}">
      <view class="user-info">
        <image class="user-avatar radius-circle" src="{{userInfo.avatarUrl}}" mode="aspectFill"></image>
        <view class="user-details">
          <view class="user-name">{{userInfo.nickName}}</view>
          <view class="user-meta text-small">
            <text>{{storeInfoText}}：{{currentStore}}</text>
          </view>
        </view>
      </view>
      <view class="settings-btn" bindtap="navigateToSettings">
        <text class="iconfont icon-settings"></text>
      </view>
    </block>
    
    <block wx:else>
      <view class="login-prompt">
        <view class="default-avatar radius-circle"></view>
        <view class="login-text">
          <text class="login-title">{{notLoggedInText}}</text>
          <text class="login-desc">{{loginDescText}}</text>
        </view>
        <button class="login-btn" bindtap="wxLogin">{{wechatLoginText}}</button>
      </view>
    </block>
  </view>

  <!-- 功能模块 -->
  <view class="function-modules">
    <view class="module-item" bindtap="navigateToBookings" wx:if="{{userInfo}}">
      <view class="module-icon">
        <text class="iconfont icon-calendar"></text>
      </view>
      <view class="module-content">
        <text class="module-title">{{myBookingsText}}</text>
        <text class="module-desc">{{viewBookingsText}}</text>
      </view>
      <text class="iconfont icon-arrow-right"></text>
    </view>

    <view class="module-item" bindtap="navigateToCourseCards" wx:if="{{userInfo}}">
      <view class="module-icon">
        <text class="iconfont icon-card"></text>
      </view>
      <view class="module-content">
        <text class="module-title">{{myCourseCardsText}}</text>
        <text class="module-desc">{{viewCourseCardsText}}</text>
      </view>
      <text class="iconfont icon-arrow-right"></text>
    </view>

    <view class="module-item" bindtap="showQRCode" wx:if="{{userInfo}}">
      <view class="module-icon">
        <text class="iconfont icon-qrcode"></text>
      </view>
      <view class="module-content">
        <text class="module-title">{{entranceCodeText}}</text>
        <text class="module-desc">{{generateCodeText}}</text>
      </view>
      <text class="iconfont icon-arrow-right"></text>
    </view>

    <view class="module-item" bindtap="contactCustomerService">
      <view class="module-icon">
        <text class="iconfont icon-service"></text>
      </view>
      <view class="module-content">
        <text class="module-title">{{customerServiceText}}</text>
        <text class="module-desc">{{contactServiceText}}</text>
      </view>
      <text class="iconfont icon-arrow-right"></text>
    </view>

    <view class="module-item" bindtap="navigateToAdmin">
      <view class="module-icon">
        <text class="iconfont icon-admin"></text>
      </view>
      <view class="module-content">
        <text class="module-title">管理员入口</text>
        <text class="module-desc">系统管理功能</text>
      </view>
      <text class="iconfont icon-arrow-right"></text>
    </view>
  </view>

  <!-- 我的预约预览 -->
  <view class="booking-preview card" wx:if="{{bookings.length > 0}}">
    <view class="preview-header">
      <text class="preview-title">{{recentBookingsText}}</text>
      <text class="preview-more" bindtap="navigateToBookings">{{viewAllText}}</text>
    </view>
    <view class="preview-list">
      <view class="preview-item" wx:for="{{bookings}}" wx:for-index="index" wx:key="bookingId" wx:if="{{index < 3}}" bindtap="navigateToCourseDetail" data-course-id="{{item.courseId}}">
        <view class="item-info">
          <view class="item-title">{{item.courseName}}</view>
          <view class="item-meta text-small">{{formatBookingTime(item)}}</view>
          <view class="item-meta text-small">{{item.storeName}} · {{item.teacherName}}</view>
        </view>
        <view class="item-status">
          <text class="status-tag {{getBookingStatusClass(item.status)}}">{{getBookingStatusText(item.status)}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 我的课程卡预览 -->
  <view class="card-preview card" wx:if="{{courseCards.length > 0}}">
    <view class="preview-header">
      <text class="preview-title">{{myCardsText}}</text>
      <text class="preview-more" bindtap="navigateToCourseCards">{{viewAllText}}</text>
    </view>
    <view class="card-list">
      <view class="card-item" wx:for="{{courseCards}}" wx:for-index="index" wx:key="userCardId" wx:if="{{index < 3}}">
        <view class="card-info">
          <view class="card-title">{{item.cardName}}</view>
          <view class="card-count">
            <text class="count-number">{{item.remainingCount}}</text>
            <text class="count-unit">{{unitText}}</text>
            <text class="count-total">/ {{item.totalCount}}</text>
          </view>
        </view>
        <view class="card-status">
          <text class="status-tag {{getCourseCardStatusClass(item.status)}}">{{getCourseCardStatusText(item.status)}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 退出登录 -->
  <view class="logout-section" wx:if="{{userInfo}}">
    <button class="logout-btn" bindtap="logout">{{logoutText}}</button>
  </view>
</view>

<!-- 加载状态 -->
<view class="loading-container" wx:if="{{loading}}">
  <view class="loading">
      <text>{{loadingText}}</text>
    </view>
</view>